# Core dependencies
aiohttp>=3.14.3
pandas>=3.0.5
openpyxl>=3.1.5
python-dotenv>=1.2.3

# OpenAI SDK for OpenRouter compatibility
openai>=3.3.1

# Utilities
beautifulsoup4>=4.15.0
rich>=15.0.0
selenium>=4.47.0
fastapi>=0.141.1
uvicorn>=0.52.4
jinja2>=3.1.6
python-multipart>=0.0.32
cryptography>=50.0.0

# Development and testing (optional)
pytest>=9.1.1
pytest-asyncio>=1.4.0
# Ruff replaces black + flake8: `ruff format` is black-compatible and
# `ruff check` covers the pycodestyle/pyflakes rules flake8 provided.
# Pinned exactly, unlike the runtime deps above: a linter that floats can
# turn CI red on a release that merely adds a rule, with no change to this
# repo. Dependabot bumps it as a reviewable PR instead.
ruff==0.16.5
